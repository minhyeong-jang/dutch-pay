import React, { FC, useState } from "react";
import styled from "styled-components";

export const UserListContainer: FC = () => {
  return <StyledContainer>User List</StyledContainer>;
};
const StyledContainer = styled.div``;
