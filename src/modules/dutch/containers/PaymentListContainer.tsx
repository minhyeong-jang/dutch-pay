import React, { FC } from "react";
import styled from "styled-components";

export const PaymentListContainer: FC = () => {
  return <StyledContainer></StyledContainer>;
};
const StyledContainer = styled.div``;
