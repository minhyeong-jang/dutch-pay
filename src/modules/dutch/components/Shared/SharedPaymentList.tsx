import React, { FC } from 'react';
import styled from 'styled-components';

export const SharedPaymentList: FC = () => {
  return <StyledContainer></StyledContainer>;
};
const StyledContainer = styled.div``;
