import React, { FC } from 'react';
import styled from 'styled-components';

export const PersonalSend: FC = () => {
  return <StyledContainer></StyledContainer>;
};
const StyledContainer = styled.div``;
